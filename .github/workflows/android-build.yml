name: Build and Release APK

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up JDK
        uses: actions/setup-java@v4 # Updated to v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Frontend & Build
        run: |
          cd client
          npm install
          echo "VITE_API_URL=${{ secrets.REACT_APP_API_URL }}" > .env
          npm run build
        
      - name: Sync Capacitor
        run: |
          npm install @capacitor/cli @capacitor/core @capacitor/android
          npx cap sync

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3 # Updated to the latest recommended action

      - name: Build Debug APK
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew assembleDebug

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2 # Updated to v2
        with:
          files: android/app/build/outputs/apk/debug/app-debug.apk
          name: Release ${{ github.ref_name }}
          body: "Official ResolveIt APK build. Generated by LYFSpot."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}