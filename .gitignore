# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies and Build Artifacts
node_modules
dist
dist-ssr
*.local
.npm
.yarn

# === CRITICAL ADDITIONS (Environment Variables & Secrets) ===
# Environment Variables (MUST BE IGNORED)
.env
.env.development
.env.production
.env.local

# 🔑 FIREBASE SECRETS (If you used the JSON file locally)
# This is where your private key would live if not using an ENV variable.
serviceAccountKey.json 

# === Build/Cache Directories ===
# VITE/Webpack Build and Cache
/build
/out
/coverage
/temp
# Next.js / other popular cache dirs
.cache
.next 

# === Operating System Files ===
.DS_Store
Thumbs.db

# === Editor directories and files ===
.vscode/*
!.vscode/extensions.json
# 👇 Preserve settings for debugging (if you use launch.json/tasks.json)
!.vscode/launch.json
!.vscode/tasks.json 
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ignore Service Account Keys
server/config/serviceAccounKey.json
server/config/serviceAccounKey.json.json